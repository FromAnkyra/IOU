import oweytable
import userdb
#import whatever the slack api stuff I need

#get the events - once the event is stored into a string (message), run this

if message == '!IOU' + Auserid + ',' + Buserid:
    sendmessage(Auserid + 'owes' Buserid + oweytable.read(Auserid, Buserid))
elif message == '!IOU' + Auserid
    #every non-zero owage
elif message == 'IOU' + Auserid + ',' Buserid + ',' + #numbers
